# bitcoin-unified-data-standard
The Bitcoin Unified Data Standard (BUDS) is a carrier-agnostic TLV framework and global registry that standardises how arbitrary on-chain data is structured, typed, decoded, and understood across wallets, nodes, explorers, L2s, and data lanes.
